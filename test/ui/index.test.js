// const { Builder, By } = require("selenium-webdriver");
// var chrome = require('selenium-webdriver/chrome');

describe('My Company', () => {
    // let _driver;

    // before(function(){
    //     debugger;
    //     _driver = new Builder().forBrowser("chrome").build();
    // });

    // after(() => _driver && _driver.quit());

    it("load page my company", function(){
        // debugger;
        // let url = "file:///".concat(__dirname).concat("dist/index.html");

        // _driver.get(url);
        // let header = _driver.findElement(By.css("header a")).innerText;

        // expect(header).toEqual("My Company");
        expect(true).toEqual(true);
    });
});
